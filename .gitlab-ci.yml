build_linux:
  script:
    - cd app/static
    - git submodule init
    - git submodule update
    - cd ../..
    - cp config.py.example config.py
    - coverage run set_up.py
lint:
  allow_failure: true
  script:
    - pylint -f parseable -rn models plugins